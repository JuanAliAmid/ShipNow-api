export const USER_ROLES = Object.freeze({
    ADMIN: 'admin',
    USER: 'customer',
    STORE: 'store'
});

export const ORDER_STATUS = Object.freeze({
    CREATED: 'created',
    PICKED_UP: 'picked_up',
    ASSIGNED: 'assigned',
    IN_TRANSIT: 'in_transit',
    DELIVERED: 'delivered',
    CANCELLED: 'cancelled'
});
